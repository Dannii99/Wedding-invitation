import { DeferOnScrollDirective } from './defer-on-scroll.directive';

describe('DeferOnScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new DeferOnScrollDirective();
    expect(directive).toBeTruthy();
  });
});
